{
  "name": "nexo-atomico-app",
  "version": "1.0.0",
  "private": true,
  "description": "El Nexo Atómico convertido en aplicación Android",
  "scripts": {
    "sync": "npx cap sync android",
    "open:android": "npx cap open android",
    "run:android": "npx cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
